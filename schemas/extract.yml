$schema: http://json-schema.org/draft-04/schema#

type: object
properties:
    inputs:
      type: object
      required:
        - body

      properties:
        body:
          type: string

    outputs:
      type: object
      additionalProperties:
        anyOf:
           - type: string
           - type: number
