$schema: http://json-schema.org/draft-04/schema#

type: object
properties:
  inputs:
    type: object
    properties:
      values:
        type: object
        additionalProperties:
          anyOf:
             - type: string
             - type: number

  options:
    type: object
    properties:
      template:
        type: string

  outputs:
    type: object
    properties:
      rendered:
        type: string
